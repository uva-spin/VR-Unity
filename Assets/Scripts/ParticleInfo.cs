using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum PartColor{Red, Blue, Green}
public class ParticleInfo : MonoBehaviour
{
    public PartColor partColor;
    public int partIdx;
}
